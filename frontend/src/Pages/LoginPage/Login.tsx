import React from 'react';
import { VStack,Heading } from '@chakra-ui/react';
interface Props {
  
}

const Login: React.FC<Props> = () => {
  return (
    <div>
      <Heading as={'h1'}>LoginPage</Heading>
    </div>
  );
};

export default Login;